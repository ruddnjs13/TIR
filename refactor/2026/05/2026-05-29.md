# 📅 2026-05-29 리팩토링

> TimeUtil 파일 분리


```c++
## 📂 대상 파일 / 모듈
- TimeController.cs

## 🔍 리팩토링 전 (문제점)
- MonoBehavior인 TimeController와 TimeUtil이 한 파일에 있다.

## ✅ 리팩토링 후 (개선 내용)
- TimeController와 TimeUtil을 별도의 파일을 분리하였다.

## 💡 배운 점 / 메모
- 같은 파일에 있어도 무방할거 같으나 유틸 클래스에 규칙을 일관성있게 유지하기 위해 분리를 해주었다.
```


```c#
## 개선전
namespace Code.TimeSystem
{
    public static class TimeUtil
    {
        //이건 현실 시간 기준임
        // ex 현실 시간 기준을 게임 기준 시간으로 변환 해줌
        public static float Min(float v) => v ;
        public static float Hour(float v) => v * 60f;
        public static float Day(float v) => v * 3600f;
    }
    
    [Provide]
    public class TimeController : MonoBehaviour, IDependencyProvider
    {
    }
}
```


```c#
## 개선후
public static class TimeUtil
    {
        //이건 현실 시간 기준임
        // ex 현실 시간 기준을 게임 기준 시간으로 변환 해줌
        public static float Min(float v) => v ;
        public static float Hour(float v) => v * 60f;
        public static float Day(float v) => v * 3600f;
    }
```

