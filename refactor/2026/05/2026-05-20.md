# 📅 2026-05-20 리팩토링

> PreviewItem 개선


```c++
## 📂 대상 파일 / 모듈
- PreviewItem.cs

## 🔍 리팩토링 전 (문제점)
- 기존 필드들과 오브젝트 풀링 관련 필드들이 섞여있어 난잡하다.
- spriteRender와 OutLineable에 null 체크가 안되고 있다.

## ✅ 리팩토링 후 (개선 내용)
- Header를 사용해 오브젝틀 풀링 관련 필드들을 모아서 가독성을 높혔다.
- debug.Assert()를 이용해 사전에 SerializeField값이 할당 돼지 않았는지를 체크하도록 했다.
- Camera.Main도 예전과 달리 내부적으로 캐싱해 두기때문에 성능 차이는 없지만 필드만 보고도
  이 클래스가 Camera.Main에 의존한다는 것을 명시적으로 나타낼 수 있어서 캐싱해 두었다.

## 💡 배운 점 / 메모
- 생각보다 기본적인 null체크가 잘 안 되어 있어 아쉬웠다.
```


```c#
## 개선전
public class PreviewItem : MonoBehaviour, IPoolable, IInteractable
{
    [SerializeField] private PoolItemSO viewItemPool;
    [SerializeField] private SpriteRenderer spriteRenderer;
    
    public ItemBase Item { get; private set; }
    private int _stack;
    private Pool _myPool;
    
    public PoolItemSO PoolItem => viewItemPool;
    public GameObject GameObject => gameObject;
    [field: SerializeField] public Outlinable Outlinable { get; private set; }
    
    public void SetUpPool(Pool pool)
    {
        _myPool = pool;
    }

    public void ResetItem()
    {
        Item = null;
        _stack = 0;
        Outlinable.enabled = false;
        spriteRenderer.sprite = null;
    }

    private void Init(ItemBase item, int stack)
    {
        if (item == null) return;

        Item = item;
        _stack = stack;
        
        if (item.ItemData.itemImage != null)
            spriteRenderer.sprite = Item.ItemData.itemImage;
        gameObject.name = $"dropItem_{Item.ItemData.itemName}";

        Outlinable.enabled = false;
    }

    public void Discard(Vector3 dropPosition, ItemBase item, int stack)
    {
        Init(item, stack);
        
        transform.forward = -Camera.main.transform.forward;
        transform.position = dropPosition;
    }

    public void Select()
    {
        Outlinable.enabled = true;
    }

    public void DeSelect()
    {
        Outlinable.enabled = false;
    }

    public void Interact(Entity interactor)
    {
        if (interactor.TryGetSubclassComponent<Inventory>(out var inventory) && inventory.TryAddItem(Item, _stack))
        {
            Item = null;
            _myPool.Push(this);
        }
    }
}
```


```c#
## 개선후
public class PreviewItem : MonoBehaviour, IPoolable, IInteractable
{
    [SerializeField] private PoolItemSO viewItemPool;
    [SerializeField] private SpriteRenderer spriteRenderer;
    private int _stack;
    private Camera _mainCamera;
    
    public ItemBase Item { get; private set; }
    [field: SerializeField] public Outlinable Outlinable { get; private set; }
    
    [Header("Pool")]
    private Pool _myPool;
    public PoolItemSO PoolItem => viewItemPool;
    public GameObject GameObject => gameObject;

    private void Awake()
    {
        _mainCamera = Camera.main;
        Debug.Assert(spriteRenderer != null, "spriteRenderer 미할당", this);
        Debug.Assert(Outlinable != null, "Outlinable 미할당", this);
    }

    private void Init(ItemBase item, int stack)
    {
        Item = item;
        _stack = stack;

        spriteRenderer.sprite = Item.ItemData.itemImage;
        gameObject.name = $"dropItem_{Item.ItemData.itemName}";
        
        Outlinable.enabled = false;
    }

    public void Discard(Vector3 dropPosition, ItemBase item, int stack)
    {
        if (item != null)
        {
            Init(item, stack);
            transform.forward = -_mainCamera.transform.forward;
            transform.position = dropPosition;
        }   
    }

    public void Select()
       => Outlinable.enabled = true;

    public void DeSelect()
       => Outlinable.enabled = false;

    public void Interact(Entity interactor)
    {
        if (interactor.TryGetSubclassComponent<Inventory>(out var inventory) 
            && inventory.TryAddItem(Item, _stack))
        {
            Item = null;
            _myPool.Push(this);
        }
    }
    
    #region Pooling
    public void SetUpPool(Pool pool)
        =>_myPool = pool;

    public void ResetItem()
    {
        Item = null;
        _stack = 0;
        
        Outlinable.enabled = false;
        spriteRenderer.sprite = null;
    }
    #endregion
}
```

