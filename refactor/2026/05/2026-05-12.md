# VendingMachine

> 내용 없음


```c++
## 📂 대상 파일 / 모듈
- VendingMachine.cs
- ItemSOPreview.cs

## 🔍 리팩토링 전 (문제점)
- VendingMachine에 조상클래스인 HittableObject에 자신이 맞는 것을 알리는 이벤트인 OnTakeDamage
  를 VendingMachine에서 직접 구독해서 쓰고 있는 문제가 있었다.
- ItemSOPreview에서 _nameLabel, _idLabel, _rootElement이 기본 private 인스턴스 여서 생성자에서
  초기화 해주는 변수가 명시적으로 표시 돼지 않는다.

## ✅ 리팩토링 후 (개선 내용)
- 조상클래스 HittalbeObject에 TakeHit()를 가상메서드로 구현하고 ApplyDamage에서 실행 시켜주었다.
  VendingMachine이 TakeHit()을 재정의하여 처리 하도록 구조를 수정 하였다.
- _nameLabel, _idLabel, _rootElement에 모두 readOnly 키워드를 붙혀서 생성자에서만 초기화 된다는
  것을 명시적으로 나타내 주었다

## 💡 배운 점 / 메모
- vendingMachine 자체가 MonoBehavior기반으로 돼있었는데 InteractableHittableObject 기반으로
  바꾸는 것을 클로드 코드에게 바이브 코딩으로 시켰는데 자신에 이벤트를 자신이 구독하는 등에 어색한
  부분이 있어서 바이브 코딩 한 코드는 무조건 한번씩 리펙토링을 해야하는 걸 다시 느꼈다.
```

