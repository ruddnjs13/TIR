# 📅 2026-06-01 리팩토링

> SectionNameText 개선


```c++
## 📂 대상 파일 / 모듈
- SectionNameText.cs

## 🔍 리팩토링 전 (문제점)
- IPoolable을 상속받아서 게임오브젝트가 생성과 삭제가 아닌 활성화 비활성화만 돼는데 이벤트에 구독과
  구독 취소를 Start()와 OnDestroy()에서 해주고 있었다. 

## ✅ 리팩토링 후 (개선 내용)
- OnEanble()과 OnDisable()에서 이벤트 구독 관리 처리를 해주도록 수정 하였다.

## 💡 배운 점 / 메모
- 사실 이건 리펙토링보다 버그가 발생할수도있는 부분을 수정한거 같다.
```


```c#
## 개선전
  private void Start()
        {
            Bus.Subscribe<ShowItemsOnMap>(HandleShowItemsOnMap);
            Bus.Subscribe<ChangePinCountEvent>(HandleChangePinCount);
        }

       
        private void OnDestroy()
        {
            Bus.Unsubscribe<ShowItemsOnMap>(HandleShowItemsOnMap);
            Bus.Unsubscribe<ChangePinCountEvent>(HandleChangePinCount);
        }
```


```c#
## 개선후
private void OnEnable()
        {
            Bus.Subscribe<ShowItemsOnMap>(HandleShowItemsOnMap);
            Bus.Subscribe<ChangePinCountEvent>(HandleChangePinCount);
        }

       
        private void OnDisable()
        {
            Bus.Unsubscribe<ShowItemsOnMap>(HandleShowItemsOnMap);
            Bus.Unsubscribe<ChangePinCountEvent>(HandleChangePinCount);
        }
```

